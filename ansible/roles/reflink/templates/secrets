export AWS_ACCESS_KEY="{{ aws_access_key }}"
export AWS_SECRET_KEY="{{ aws_secret_key }}"
export AWS_REGION="{{ aws_region }}"
export REFLINK_DYNAMODB_ENDPOINT="{{ dynamodb_endpoint }}"
export REFLINK_S3_ENDPOINT="{{ s3_endpoint }}"
export REFLINK_S3_BUCKET="{{ s3_bucket }}"
export REFLINK_SQS_ENDPOINT="{{ sqs_endpoint }}"
export REFLINK_KINESIS_ENDPOINT="{{ kinesis_endpoint }}"
export AWS_CBOR_DISABLE=1
export BOTO_ENDPOINTS=/opt/reflink/endpoints.json
