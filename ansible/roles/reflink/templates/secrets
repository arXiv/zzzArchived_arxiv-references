export AWS_ACCESS_KEY="{{ aws_access_key }}"
export AWS_SECRET_KEY="{{ aws_secret_key }}"
export AWS_REGION="{{ aws_region }}"
export REFLINK_AWS_ACCESS_KEY="{{ aws_access_key }}"
export REFLINK_AWS_SECRET_KEY="{{ aws_secret_key }}"
export REFLINK_AWS_REGION="{{ aws_region }}"
export REFLINK_SQS_ENDPOINT="sqs://{{ aws_access_key }}:{{ aws_secret_key }}@"
export REFLINK_S3_BUCKET="{{ s3_bucket }}"
export APPLICATION_ROOT='{{ application_root }}'
export PYCURL_SSL_LIBRARY=nss
export REFLINK_CERMINE_DOCKER_IMAGE="{{ cermine_docker_image }}"
export REFLINK_AUTOTEX_DOCKER_IMAGE="{{ autotex_docker_image }}"
export REFLINK_GROBID_DOCKER_IMAGE="{{ grobid_docker_image }}"
export REFLINK_SCIENCEPARSE_DOCKER_IMAGE="{{ scienceparse_docker_image }}"
export REFLINK_BASE_URL="{{ reflink_base_url }}"
